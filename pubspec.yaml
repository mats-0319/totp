name: totp
description: ""
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  base32: ^2.2.0
  crypto: ^3.0.7
  flutter:
    sdk: flutter

  markdown_widget: ^2.3.2+8
  path_provider: ^2.1.5
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  otp: ^3.2.0
  test: ^1.26.2

flutter:
  assets:
    - assets/avatar_256.jpg
    - assets/manual.md
    - assets/tech.md
    - assets/totp_design.png

  uses-material-design: true
